/*
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'
import { BrowserRouter } from 'react-router-dom' // Importando para usar o react-router.

//Importa o provider do react-redux para englobar toda a sua aplicação. Para que todo o conteudo tenha acesso ao seus dados gerenciados.
import { Provider } from 'react-redux'

//Importa o store do arquivo store que voce crio e configuro, para que todo o seu conteudo que estiver dentro do seu provider tenha acesso ao store. Através de uma prop , voce somente passa essa 1 prop aqui que ele vai ter acesso a tudo que voce for criar e manipular na sua aplicação.
import { store } from './redux/store'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <Provider store={store}>
      <BrowserRouter >
          <App />
      </BrowserRouter>
    </Provider>
    
  </React.StrictMode>,
)
*/